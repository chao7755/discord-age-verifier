discord.py==2.3

# ---------- OCR (雲端) ----------
requests==2.32.3     # 呼叫外部 OCR API

# ---------- 影像處理 ----------
Pillow==10.3.0        # 把使用者上傳的圖片轉成 JPG 再送 API

# ---------- Web keep-alive ----------
flask==3.0.2
waitress==3.0.0
